{{#if network}}
    {{#if username}}
        {{!-- {{#if (eq (lowercase network) "linkedin")}}
            {{> info-tag text=(concat '' this.username) icon="fa-linkedin-square" }}
        {{else}} --}}
        {{> info-tag text=this.username name=network}}
        {{!-- {{/if}} --}}
    {{/if}}
{{/if}}
