{{#if network}}
    {{#if username}}
        {{!-- {{#if (eq (lowercase network) "linkedin")}}
            {{> info-tag text=(concat '' this.username) icon="fa-linkedin-square" }}
        {{else}} --}}
        {{> info-tag text=(concat network ': ' this.username) icon=(concat "fa-" (lowercase network)) }}
        {{!-- {{/if}} --}}
    {{/if}}
{{/if}}
