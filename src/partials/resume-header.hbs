{{#resume.basics}}
<header class="text-center mb-8 pb-6 border-b border-gray-200">
    <div class="space-y-3">
        <h1 class="text-4xl font-heading font-bold text-gray-900 tracking-wide">{{name}}</h1>
        <h2 class="text-xl font-heading text-gray-600 font-light tracking-wide">{{label}}</h2>
    </div>
</header>
{{/resume.basics}}
