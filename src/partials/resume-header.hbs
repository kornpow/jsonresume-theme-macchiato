{{#resume.basics}}
<header class="text-center mb-6 pb-4 border-b border-gray-200">
    <div class="space-y-2">
        <h1 class="text-4xl font-bold text-gray-900 tracking-wide">{{name}}</h1>
        <h2 class="text-lg text-gray-600 font-light tracking-wide">{{label}}</h2>
    </div>
</header>
{{/resume.basics}}
